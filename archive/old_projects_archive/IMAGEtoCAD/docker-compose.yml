version: "3.8"

services:
  imagetocad:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./outputs:/app/outputs
      - easyocr_cache:/root/.EasyOCR
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  easyocr_cache:
